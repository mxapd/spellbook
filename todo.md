there is still the issue with not showing the first selected item when searching for commands, that is when just searching :. Also when using enter to select a command it doesnt execute and trying to do that seems to just enter a spellbook behind the search.

also write down a todo to make the deadspace that appears after the cards to be split on both left and right side of the cards, basicly center aligning the whole card 'container'.
